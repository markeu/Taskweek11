export type signupType = {
  firstName?: string;
  lastName?: string;
  email: string;
  password: string;
};

export type signIntype = {};

export type orgInterface = {
  organization: string;
  marketValue: string;
  address: string;
  ceo: string;
  products: string;
  employees: string;
};
